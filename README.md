# vagrant-ansible-for-dev-envs
A collection of Vagrantfiles and Ansible playbooks to configure a portable development environment in a VM.

## Requirements
- Virtualbox 6.1
- Vagrant 

## Usage
Once installed Vagrant, put this files in a folder and run via terminal (Powershell for Windows, Shell for Unix):

```vagrant up```
